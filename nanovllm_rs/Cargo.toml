[package]
name = "nanovllm_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
candle-core = "0.8"
candle-nn = "0.8"
cudarc = { version = "0.19.8", features = ["nccl"] }
half = "2"

[features]
cuda = ["candle-core/cuda"]
